# CONTROL / LEDGER — Live State, Verdicts, Restart Truth

Project: Candice Companion AI (spec-protocol build, 999-setup repo)
Canonical Master Spec: `/Users/blackceomacmini/Downloads/CANDICE_COMPANION_AI_IMPLEMENTATION_SPEC_V6_FINAL_LOCKED.md` (canonicalized as `SPEC/MASTER-SPEC-2026-08-21.md` — see SESSION-LOG)
Last updated: 2026-08-21 (UTC ~13:31)

---

## 1. CURRENT STATE VIEW (regenerated; the first operational truth)

| Field | Value |
|---|---|
| Logical wave | pre-dispatch (wave 0; WR-002 assembled, not yet dispatched) |
| Execution epoch | 0 |
| Safe live width | **pending capacity run** — no width committed until CAPACITY-LEDGER computation completes |
| Intended workflow runs | 0 (board not yet populated with future intents) |
| Visible workflow runs | 0 |
| Active workflow runs | 2 (WR-001 completed, WR-002 in flight/assembled) |
| Completed workflow runs | 1 (WR-001 — candice-apparatus-baseline, 5/5 OK) |
| Integration SHA | `6bb00ec70af69510fab5a9c2ef332751e260d036` (HEAD of main, verified 2026-08-21) |
| Pending builder handoffs | none |
| Pending rechecks | none |
| Severe blockers | none |

### Exact next conductor actions (in order)

1. Complete the capacity run → write CAPACITY-LEDGER.md → set safe live width.
2. Dispatch WR-002 (candice-apparatus-baseline) per dispatch-log.md.
3. Move WR-001 / WR-002 rows per EXECUTION-PLAN.md board rules.
4. Continue waves per EXECUTION-PLAN.md step 3/4.

---

## 2. QC VERDICT BLOCKS (durable)

None yet. No QC verdicts issued for the Candice build as of 2026-08-21.

---

## 3. MERGE / RELEASE RECORDS

None yet. No merges or releases for the Candice build as of 2026-08-21.

---

## 4. LITERAL RESTART STEPS (cold resuming conductor — run in this order)

1. **Read this file** (`CONTROL/LEDGER.md`) — current state + verdicts + merge record (above).
2. **Read the workflow board** — `CONTROL/EXECUTION-PLAN.md`, named section "Workflow Launch Board". This is the live board of intended/visible/active/completed workflow runs.
3. **Read machine truth** — `CONTROL/project_state.json`. Note: current file still carries the prior `999-master-fix` project field from an earlier run — see SESSION-LOG 2026-08-21 entry "999-master-fix residue". Do not trust its workflow-run counts for the Candice build; this ledger (section 1) is authoritative for counts until the file is regenerated by a conductor.
4. **Re-fetch main per spec 0G freshness** — run `git fetch origin main` (or `git pull --ff-only` on main) in `/Users/blackceomacmini/Downloads/999-setup`.
5. **Verify SHAs** — confirm the working tree HEAD matches the integration SHA in section 1 (`6bb00ec70af69510fab5a9c2ef332751e260d036`). If it differs, the newer SHA wins: update section 1 and the board, and note the delta in SESSION-LOG.
6. **Resume dispatch** — resume at spec step 3/4 (wave dispatch and completion per EXECUTION-PLAN.md): next action is the capacity run, then dispatch WR-002 (candice-apparatus-baseline, units listed in `CONTROL/dispatch-log.md`).
7. If the ledger was interrupted mid-write, re-verify pending builder handoffs and pending rechecks against TODO/CHECKLIST before dispatching anything.
