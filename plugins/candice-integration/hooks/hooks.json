{
  "description": "Candice wake-up hook. It inspects only the submitted prompt's first token and wakes for the four supported slash commands. The dispatcher requests a fail-soft visual wake; it does not bind a Claude session or terminal host until an authenticated runtime boundary exists.",
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/bin/wake-candice.mjs\" --from-prompt",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
