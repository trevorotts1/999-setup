[package]
name = "candice-release-config-guard"
version = "0.1.0"
edition = "2021"
description = "Q-10 guard: fails hard when a release build config would let tauri-bundler emit an unsigned updater artifact, and refuses a smoke build that claims updater-ready posture."
license = "MIT"

[dependencies]
minisign = "0.7.9"
serde_json = "1"
base64 = "0.22"
