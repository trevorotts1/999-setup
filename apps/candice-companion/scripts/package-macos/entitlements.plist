<!-- WS-23 — hardened-runtime baseline. Owned lane: apps/candice-companion/scripts/package-macos/** (PROJECT-MANIFEST 9.2, WR-015 row, WS-23 glob). -->
<!-- com.apple.security.device.audio-input is what actually grants the
     microphone. Info.plist already carries NSMicrophoneUsageDescription, but
     that string is only what the user is SHOWN when the system asks; it does
     not grant anything. Under the hardened runtime the request is refused
     before any prompt appears unless this entitlement is present, and the
     refusal is indistinguishable from a broken microphone.

     This build is hardened today: codesign reports flags=0x10002 (adhoc,
     runtime). It shipped carrying only the three cs.* keys, so push to talk
     could not work in the packaged app at all. Dev runs use an unhardened
     binary, which is why nothing here ever felt it.

     Note for whoever adds keys next: an XML comment may not contain a double
     hyphen, and codesign fails with "AMFIUnserializeXML: syntax error" if one
     does. That is a signing failure, not a plist failure, and plutil -lint
     will happily call the file OK first. -->
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>com.apple.security.cs.allow-jit</key>
	<false/>
	<key>com.apple.security.cs.allow-unsigned-executable-memory</key>
	<false/>
	<key>com.apple.security.cs.disable-library-validation</key>
	<false/>
	<key>com.apple.security.device.audio-input</key>
	<true/>
</dict>
</plist>
