{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Candice main window shell capabilities (WS-06). Window visibility primitives only; session bridge tools (candice.* MCP, spec 13.2) are granted by the plugin lane (WR-011).",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-set-always-on-top",
    "core:window:allow-start-dragging",
    "core:window:allow-set-ignore-cursor-events",
    "updater:default"
  ]
}
