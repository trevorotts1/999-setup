<!-- template: document 12 (references/documents.md). Copy to CONTROL/dispatch-log.md, replace every <fill>, delete this line. -->
# Dispatch log

One row per dispatch, written by tools/dispatch-check.sh BEFORE the agents fire. Never hand-written.
