<!-- template: document 13 (references/documents.md). Copy to CONTROL/HEARTBEAT.md, replace every <fill>, delete this line. -->
# Heartbeat

One line per live agent, written only through tools/ledger.sh upsert mode with the agent label as the key.
